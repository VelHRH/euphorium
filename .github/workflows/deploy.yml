name: Deploy Server and Frontend to EC2

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create environment files
        run: |
          echo "${{ secrets.BACKEND_ENV_FILE }}" > server/.env
          echo "${{ secrets.FRONTEND_ENV_FILE }}" > front/.env

      - name: Build and push Server Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./server
          file: ./server/docker/Dockerfile.server
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/newblood-server:latest

      - name: Build and push Frontend Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./front
          file: ./front/docker/Dockerfile.front
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/newblood-front:latest

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Create environment files
            echo "${{ secrets.BACKEND_ENV_FILE }}" > backend.env
            echo "${{ secrets.FRONTEND_ENV_FILE }}" > frontend.env
            
            # Pull latest images
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/newblood-server:latest
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/newblood-front:latest
            
            # Stop and remove existing containers
            docker stop newblood-server newblood-front || true
            docker rm newblood-server newblood-front || true
            
            # Start backend container
            docker run -d --name newblood-server \
              --env-file backend.env \
              -p 8090:8090 \
              ${{ secrets.DOCKERHUB_USERNAME }}/newblood-server:latest

            # Start frontend container
            docker run -d --name newblood-front \
              --env-file frontend.env \
              -p 3001:3001 \
              ${{ secrets.DOCKERHUB_USERNAME }}/newblood-front:latest